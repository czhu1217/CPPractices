#include <iostream>
#include <stdio.h>
#include <cstring>
#include <algorithm>
#include <vector>
#include <math.h>
#include <map>
using namespace std;
int N, K, a[1002], ans, rem[1003], mm=0, sum;
int main(){
    cin >> N >> K;
    for(int i=1, t;i<=N;i++){
        scanf("%d", &a[i]);
        mm = max(mm, a[i]);
    }
    for(int i=1;i<=mm;i++){
        int sum = 0;
        for(int j=1;j<=N;j++){
            sum +=
        }
    }



}