#include <iostream>
#include <stdio.h>
#include <cstring>
#include <algorithm>
#include <vector>
#include <math.h>
#include <map>
#include <set>
#include <unordered_set>
#include <bitset>
#include <queue>
#include <unordered_map>
typedef long long ll;
#define f first
#define s second
using namespace std;
const int MM = 1e5+2;
pair<int, int> qu[MM];
int n, q, m[MM], v[MM];
int main(){
    cin >> n >> q;
    for(int i=1;i<=n;i++){
        cin >> m[i];
    }
    for(int i=1;i<=n;i++){
        cin >> v[i];
    }
    for(int i=1;i<=q;i++){
        cin >> qu[i].f >> qu[i].s;
    }
    



}