#include <stdio.h>
int N;
char a[1000005], preg, pres, sufg, sufs;
long long asns = 0;
int main(){
    scanf("%d", &N);
    scanf(" %c", &a[0]);
    for(int i=1;i<N;i++){
        scanf(" %c", &a[i]);
    }
    
    return 0;

}